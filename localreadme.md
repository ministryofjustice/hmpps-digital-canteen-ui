1: Start docker desktop if not already running
2: Run `docker compose up -d` in terminal
3: Run `npm run start:dev` in terminal
4: Application should be running and accessible at http://localhost:3000
5: login details, Username 'AUTH_USER', password 'password123456'


Sometimes random auth issue displays, just http://localhost:3000/sign-out if that happens
